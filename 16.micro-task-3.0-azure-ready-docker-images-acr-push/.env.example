# .env.example – Micro-Task 3.0 (ACR Publishing)
#
# Copy this file to `.env` and adjust values for your environment.
#
# This is not used by Docker or Azure automatically unless you wire it in
# via your shell or scripts. It is here to standardise local config.

# Azure Container Registry name (no domain, just resource name)
ACR_NAME=mycompanyai

# ACR login server (usually <ACR_NAME>.azurecr.io)
ACR_LOGIN_SERVER=mycompanyai.azurecr.io

# Image versions for this micro-task
PYTHON_API_VERSION=0.1.0
FASTIFY_SERVICE_VERSION=0.1.0